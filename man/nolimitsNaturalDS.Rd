% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nolimitsNaturalDS.R
\name{nolimitsNaturalDS}
\alias{nolimitsNaturalDS}
\title{Natural-scale parameters for one transformed-scale parameter vector}
\usage{
nolimitsNaturalDS(prep.name, theta)
}
\arguments{
\item{prep.name}{name of the symbol holding a cache built by
\code{\link[=nolimitsPrepDS]{nolimitsPrepDS()}} or \code{\link[=nolimitsPrepStringDS]{nolimitsPrepStringDS()}}.}

\item{theta}{the transformed-scale parameter vector, \code{sprintf("\%a", .)}-encoded
and comma-joined.}
}
\value{
A named list with \code{natural}, a numeric vector of length \code{p}.
}
\description{
Aggregate method. Applies the model's own inverse parameter transform to the
vector the client supplied and returns the result. It is a deterministic
function of its argument and the model, and it reads no data at all: the
returned value is the same on a server holding a million subjects and on one
holding none. It exists because the client has no Julia, and because the
differentially private fit path deliberately never asks for a value or
gradient at the final parameter vector.
}
\details{
This is the same release the non-private fit path already makes on every
round, where it travels inside \code{\link[=nolimitsObjGradDS]{nolimitsObjGradDS()}}'s \code{natural} element.
}
