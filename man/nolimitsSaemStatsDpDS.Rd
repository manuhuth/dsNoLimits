% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nolimitsSaemStatsDpDS.R
\name{nolimitsSaemStatsDpDS}
\alias{nolimitsSaemStatsDpDS}
\title{Differentially private SAEM sufficient statistics}
\usage{
nolimitsSaemStatsDpDS(cache.name, theta, round = 1L)
}
\arguments{
\item{cache.name}{name of the symbol holding a cache that \code{\link[=nolimitsEmEStepDS]{nolimitsEmEStepDS()}}
has run this outer iteration.}

\item{theta}{the transformed-scale parameter vector, \code{sprintf("\%a", .)}-encoded
and comma-joined.}

\item{round}{the release index within one outer iteration, used for
validation only. The whole-fit privacy cap is enforced by the persistent
epsilon ledger, which accumulates across every release regardless of this
value; it is not a fit-wide counter.}
}
\value{
A named list with \code{stats} (the clipped, noised sum), \code{releases},
\code{epsilon}, \code{remaining.budget}, \code{delta} and \code{n.subjects}.
}
\description{
Aggregate method, the differentially private counterpart of
\code{\link[=nolimitsSaemStatsDS]{nolimitsSaemStatsDS()}}. At the FIXED draws cached by \code{\link[=nolimitsEmEStepDS]{nolimitsEmEStepDS()}},
returns the sum over this server's subjects of their DE-NORMALIZED
per-subject-additive SAEM sufficient statistics, each L2-clipped as one block
to \code{dp.clip}, plus Gaussian noise of standard deviation \code{sigma * dp.clip}.
Adding or removing one subject moves the un-noised sum by at most \code{dp.clip}, so
this is the Gaussian mechanism at noise multiplier \code{sigma}.
}
\details{
The coordinator's closed-form M-step (\code{\link[=nolimitsSaemMstepDS]{nolimitsSaemMstepDS()}}) runs on these
NOISED summed stats and is therefore post-processing: it releases nothing and
charges no budget. Every mechanism parameter is owner-set through the
\verb{dsNoLimits.dp.*} options, and each release is charged write-ahead to the
persistent ledger and refused once the owner's epsilon budget is reached.

Requires NoLimits >= 0.2.7 (the SAEM dev_api primitives).
}
