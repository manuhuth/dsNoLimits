% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nolimitsEmQDpDS.R
\name{nolimitsEmQDpDS}
\alias{nolimitsEmQDpDS}
\title{Differentially private MCEM/SAEM M-step part gradient}
\usage{
nolimitsEmQDpDS(cache.name, theta, part, round = 1L, free.names = "")
}
\arguments{
\item{cache.name}{name of the symbol holding a cache that \code{\link[=nolimitsEmEStepDS]{nolimitsEmEStepDS()}}
has run at least once this outer iteration.}

\item{theta}{the transformed-scale parameter vector, \code{sprintf("\%a", .)}-encoded
and comma-joined.}

\item{part}{\code{"q1"} or \code{"q2"}.}

\item{round}{the Adam step index within one M-step call (1 to \code{dp.rounds}),
used for validation only. The whole-fit privacy cap is enforced by the
persistent epsilon ledger, which accumulates across every release
regardless of this value; it is not a fit-wide counter.}

\item{free.names}{optional comma-joined subset of the part's free names, in the
order the gradient should be returned; \code{""} (the default) means the whole
part.}
}
\value{
A named list with \code{grad} (the clipped, noised sum), \code{releases},
\code{epsilon}, \code{remaining.budget}, \code{delta} and \code{n.subjects}.
}
\description{
Aggregate method, the differentially private counterpart of \code{\link[=nolimitsEmQDS]{nolimitsEmQDS()}}
for the DP-Adam M-step. At the FIXED draws cached by \code{\link[=nolimitsEmEStepDS]{nolimitsEmEStepDS()}},
returns the sum over this server's subjects of their per-subject gradient over
one parameter part (\code{"q1"} or \code{"q2"}, optionally restricted to a subset of free
names for the SAEM numerical M-step), each scaled by the preconditioning the
client's Adam optimiser steps in and then L2-clipped as one block to \code{dp.clip},
plus Gaussian noise of standard deviation \code{sigma * dp.clip}. Adding or removing
one subject moves the un-noised sum by at most \code{dp.clip}, so this is the
Gaussian mechanism at noise multiplier \code{sigma}.
}
\details{
NO Q value is released: a value would be a second, separately accountable
function of the data (matching \code{\link[=nolimitsObjGradDpDS]{nolimitsObjGradDpDS()}}). Every mechanism
parameter is owner-set through the \verb{dsNoLimits.dp.*} options, and each release
is charged write-ahead to the persistent ledger and refused once the owner's
epsilon budget is reached.

Requires NoLimits >= 0.2.7 (the MCEM dev_api primitives).
}
