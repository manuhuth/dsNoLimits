% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nolimitsSaemMstepDS.R
\name{nolimitsSaemMstepDS}
\alias{nolimitsSaemMstepDS}
\title{SAEM coordinator closed-form M-step (stateful)}
\usage{
nolimitsSaemMstepDS(cache.name, theta, summed.stats, outer.iter, maxiters)
}
\arguments{
\item{cache.name}{name of the symbol holding a cache that \code{\link[=nolimitsEmEStepDS]{nolimitsEmEStepDS()}}
has run this outer iteration; the update is bound back to it.}

\item{theta}{the transformed-scale parameter vector, \code{sprintf("\%a", .)}-encoded
and comma-joined.}

\item{summed.stats}{the client's coordinate-wise sum of the per-site flat
sufficient statistics, \code{sprintf("\%a", .)}-encoded and comma-joined.}

\item{outer.iter}{the outer EM iteration, a whole number starting at 1; feeds
the SA gamma schedule.}

\item{maxiters}{the fixed outer budget, a whole number; parameterizes the SAEM
method (its gamma schedule phase boundaries), matching the pooled fit.}
}
\value{
the fit cache, with the closed-form update and the smoothed_state added,
bound to \code{cache.name} by the engine.
}
\description{
Assign method, run on the COORDINATOR site only. Takes the client's summed flat
sufficient statistics, reconstructs the aggregated stats from a local template
(built from this site's own cached draws), stochastic-approximation-smooths them
against the cache's carried \code{smoothed_state} at this outer iteration's gamma, and
closed-form updates the eligible parameters (via NoLimits \code{saem_closed_form_mstep},
bit-identical to the SAEM fit). The new smoothed_state and the transformed-scale
parameter updates are written into the cache; \code{\link[=nolimitsSaemUpdatesDS]{nolimitsSaemUpdatesDS()}} reads the
updates back out (the assign-updates-state / aggregate-reads-result pattern, since
the closed-form M-step is stateful). On \code{outer.iter == 1} the SA state initializes
from the current stats (\code{smoothed_state = nothing}).
}
\details{
Requires NoLimits >= 0.2.7 (the SAEM dev_api primitives).
}
