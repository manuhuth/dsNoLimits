% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nolimitsPrepInfoDS.R
\name{nolimitsPrepInfoDS}
\alias{nolimitsPrepInfoDS}
\title{Report the model-derived metadata of a fit cache}
\usage{
nolimitsPrepInfoDS(prep.name)
}
\arguments{
\item{prep.name}{name of the symbol holding a cache built by
\code{\link[=nolimitsPrepDS]{nolimitsPrepDS()}} or \code{\link[=nolimitsPrepStringDS]{nolimitsPrepStringDS()}}.}
}
\value{
A named list with \code{theta0}, \code{names}, \code{scale}, \code{p}, \code{n.subjects},
\code{n.obs}, \code{model.hash} and \code{versions}. \code{model.hash} is the SHA-256 of the
canonical model source, which the client compares across sites to prove
every site prepared the byte-identical registered model.
}
\description{
Aggregate method. Returns everything in the cache except the Julia handle.
Every element is model-derived or a plain count; nothing is per-subject.
}
