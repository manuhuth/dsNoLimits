% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nolimitsSaemUpdatesDS.R
\name{nolimitsSaemUpdatesDS}
\alias{nolimitsSaemUpdatesDS}
\title{Read back the SAEM coordinator closed-form update}
\usage{
nolimitsSaemUpdatesDS(cache.name)
}
\arguments{
\item{cache.name}{name of the symbol holding a cache that \code{\link[=nolimitsSaemMstepDS]{nolimitsSaemMstepDS()}}
has just updated this outer iteration.}
}
\value{
A named list with \code{names} (character) and \code{values} (numeric,
transformed scale), of equal length.
}
\description{
Aggregate method, run on the COORDINATOR site only, immediately after
\code{\link[=nolimitsSaemMstepDS]{nolimitsSaemMstepDS()}} has written the update into the cache. Returns the
closed-form-eligible parameter names and their TRANSFORMED-scale values so the
client can write them into its parameter vector. This is the read half of the
assign-updates-state / aggregate-reads-result pattern the stateful closed-form
M-step needs. Everything returned is model-derived; nothing is per-subject.
}
\details{
Requires NoLimits >= 0.2.7 (the SAEM dev_api primitives).
}
