% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nolimitsModelsDS.R
\name{nolimitsModelsDS}
\alias{nolimitsModelsDS}
\title{List the models this server has registered for strict mode}
\usage{
nolimitsModelsDS()
}
\value{
A named list with \code{models} (character vector of model names, without
the \code{.jl} extension), \code{hashes} (the SHA-256 of each model's canonical
source, aligned to \code{models}) and \code{model.dir.set} (logical). Names and hashes
are model-derived only; no data is touched. An auditor compares the hashes
across nodes to see exactly which model each site has registered.
}
\description{
Touches no data. The registry is the directory named by the
\code{dsNoLimits.modelDir} option (live name first, then \code{default.}-prefixed).
Only files whose names are plain model names are listed.
}
