% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nolimitsEmEStepDS.R
\name{nolimitsEmEStepDS}
\alias{nolimitsEmEStepDS}
\title{Run one MCEM E-step and cache the draws in the session}
\usage{
nolimitsEmEStepDS(
  cache.name,
  theta,
  outer.iter,
  base.seed,
  site.id,
  sample.schedule = 100
)
}
\arguments{
\item{cache.name}{name of the symbol holding a cache built by
\code{\link[=nolimitsPrepDS]{nolimitsPrepDS()}} or \code{\link[=nolimitsPrepStringDS]{nolimitsPrepStringDS()}}; the E-step result is bound
back to it.}

\item{theta}{the transformed-scale parameter vector, \code{sprintf("\%a", .)}-encoded
and comma-joined.}

\item{outer.iter}{the outer EM iteration, a whole number starting at 1.}

\item{base.seed}{the base E-step RNG seed, a whole number.}

\item{site.id}{this site stable integer id, a whole number; added to
\code{base.seed} to seed the RNG.}

\item{sample.schedule}{the number of posterior draws per subject per E-step, a
whole number (the demo default is 100).}
}
\value{
the fit cache, with the E-step draws and warm-start state added, bound
to \code{cache.name} by the engine.
}
\description{
Assign method. At the broadcast parameter vector each site draws its own
subjects posterior random effects (via NoLimits \code{mcem_e_step}), threading the
warm-start state from the previous outer iteration, and stores the FIXED draws
plus the new state inside the fit cache. The draws never cross the wire and
nothing whose size depends on the number of subjects is returned: this method
only mutates session state, so it is an assign, and the engine binds the
returned cache back to the analyst symbol.
}
\details{
On \code{outer.iter == 1} the E-step seeds from the prior mean (state = nothing),
which also resets the sampler when a second fit reuses the same cache symbol.
The RNG is seeded by \code{base.seed + site.id}, so with each site given a stable
id the draws are a pure function of the seed, the site and the subject, and the
federated fit reproduces the pooled one up to Monte-Carlo noise.

Requires NoLimits >= 0.2.7 (the MCEM dev_api primitives); an older server
fails with a legible message.
}
