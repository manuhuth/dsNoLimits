% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nolimitsEmQDS.R
\name{nolimitsEmQDS}
\alias{nolimitsEmQDS}
\title{MCEM M-step Q value and gradient over one parameter part}
\usage{
nolimitsEmQDS(cache.name, theta, part, round = 1L, free.names = "")
}
\arguments{
\item{cache.name}{name of the symbol holding a cache that \code{\link[=nolimitsEmEStepDS]{nolimitsEmEStepDS()}}
has run at least once this outer iteration.}

\item{theta}{the transformed-scale parameter vector, \code{sprintf("\%a", .)}-encoded
and comma-joined.}

\item{part}{\code{"q1"} or \code{"q2"}.}

\item{round}{the federated round number, 1 to the \code{dsNoLimits.maxRounds}
option.}

\item{free.names}{optional comma-joined subset of the part's free names, in the
order the gradient should be returned. The SAEM numerical M-step uses it to
restrict the Q to the non-closed-form parameters of the part; \code{""} (the
default, used by MCEM) means the whole part.}
}
\value{
A named list with \code{value} (double or NA), \code{grad} (numeric, length of
the requested free names, zeros if non-finite), \code{finite} (logical) and
\code{n.subjects} (integer).
}
\description{
Aggregate method, the hot path of the federated MCEM M-step. At the FIXED
draws cached by \code{\link[=nolimitsEmEStepDS]{nolimitsEmEStepDS()}}, returns this server contribution to the
Monte-Carlo Q-function and its gradient over one part of the parameter vector
(\code{"q1"}, the observation-side names, or \code{"q2"}, the random-effect-distribution
names, from \code{\link[=nolimitsPrepInfoDS]{nolimitsPrepInfoDS()}}). The value is on the MAXIMIZATION sense and
the gradient is on the transformed axes of that part free names; the client
sums both over sites, which reproduces the pooled Q at the same draws exactly
(per-subject additive). Nothing whose size depends on the number of subjects is
returned.
}
\details{
A non-finite Q at an out-of-domain probe is a legitimate estimator result, not
a server failure: it is reported with \code{finite = FALSE} and a zero gradient so
the client optimiser backtracks. Only genuine errors stop.

Requires NoLimits >= 0.2.7 (the MCEM dev_api primitives).
}
