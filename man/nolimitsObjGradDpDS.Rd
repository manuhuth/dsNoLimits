% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nolimitsObjGradDpDS.R
\name{nolimitsObjGradDpDS}
\alias{nolimitsObjGradDpDS}
\title{Differentially private gradient of this server's data at one parameter vector}
\usage{
nolimitsObjGradDpDS(prep.name, theta, estimator, ghq.level = 1L, round = 1L)
}
\arguments{
\item{prep.name}{name of the symbol holding a cache built by
\code{\link[=nolimitsPrepDS]{nolimitsPrepDS()}} or \code{\link[=nolimitsPrepStringDS]{nolimitsPrepStringDS()}}.}

\item{theta}{the transformed-scale parameter vector, \code{sprintf("\%a", .)}-encoded
and comma-joined.}

\item{estimator}{one of \code{"laplace"}, \code{"focei"}, \code{"ghq"} or \code{"mle"}.
\code{"pooled"} and \code{"map"} are refused: the naive-pooled objective calibrates
its plug-in random effects on the whole data set and has no per-subject
term, and the MAP log-prior is not a per-subject term either, so neither
has a bounded per-subject sensitivity.}

\item{ghq.level}{Gauss-Hermite quadrature level, 1 to 9. Ignored unless
\code{estimator} is \code{"ghq"}.}

\item{round}{the fit's round number, 1 to the \code{dsNoLimits.dp.maxT} option.}
}
\value{
A named list with \code{grad} (the clipped, noised sum), \code{releases} (how
many releases this data set has been charged, ever), \code{epsilon},
\code{remaining.budget}, \code{delta} and \code{n.subjects}.
}
\description{
Aggregate method. The differentially private counterpart of
\code{\link[=nolimitsObjGradDS]{nolimitsObjGradDS()}}, and a separately permittable method: a data owner who
does not want DP analysis simply leaves it out of the profile, and one who
wants ONLY DP analysis leaves \code{nolimitsObjGradDS} out instead.
}
\details{
The release is the sum over this server's subjects of their per-subject
gradient, each L2-clipped in the preconditioned transformed coordinates the
client optimises in, plus Gaussian noise of standard deviation
\code{sigma * C_total}. Adding or removing one subject moves the un-noised sum by
at most \code{C_total}, so this is the Gaussian mechanism at noise multiplier
\code{sigma}. NO objective value is released: the value would be a second,
separately accountable function of the data.

Every parameter of the mechanism is set by the DATA OWNER through the
\verb{dsNoLimits.dp.*} options. The analyst sets none of them and cannot see them
except through the epsilon this function reports.

Each release is charged to an append-only ledger keyed by a fingerprint of
the data and the model, BEFORE the value is returned, and is refused once the
owner's cumulative epsilon budget is reached. The ledger lives on disk, so
logging out and back in does not reset it.
}
