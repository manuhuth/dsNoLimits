% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nolimitsSaemStatsDS.R
\name{nolimitsSaemStatsDS}
\alias{nolimitsSaemStatsDS}
\title{SAEM per-subject-additive sufficient statistics}
\usage{
nolimitsSaemStatsDS(cache.name, theta, round = 1L)
}
\arguments{
\item{cache.name}{name of the symbol holding a cache that \code{\link[=nolimitsEmEStepDS]{nolimitsEmEStepDS()}}
has run this outer iteration.}

\item{theta}{the transformed-scale parameter vector, \code{sprintf("\%a", .)}-encoded
and comma-joined.}

\item{round}{the federated round number, 1 to the \code{dsNoLimits.maxRounds}
option.}
}
\value{
A named list with \code{stats} (a fixed-length finite numeric vector) and
\code{n.subjects} (integer).
}
\description{
Aggregate method, the SAEM stats round. At the FIXED draws cached by
\code{\link[=nolimitsEmEStepDS]{nolimitsEmEStepDS()}}, returns this server's DE-NORMALIZED per-subject-additive
SAEM sufficient statistics as one flat numeric vector (RE moments as
\code{Sum-x = mean*n} and \code{Sum-xx = second*n} plus \code{n}; outcome and HMM fields are
plain sums). The layout is model-derived and identical on every site, so the
client sums the vectors coordinate-wise and the coordinator re-normalizes. The
length depends on the model, never on the number of subjects.
}
\details{
Requires NoLimits >= 0.2.7 (the SAEM dev_api primitives).
}
