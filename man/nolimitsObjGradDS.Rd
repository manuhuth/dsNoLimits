% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nolimitsObjGradDS.R
\name{nolimitsObjGradDS}
\alias{nolimitsObjGradDS}
\title{Marginal log-likelihood and gradient of this server's data at one parameter vector}
\usage{
nolimitsObjGradDS(prep.name, theta, estimator, ghq.level = 1L, round = 1L)
}
\arguments{
\item{prep.name}{name of the symbol holding a cache built by
\code{\link[=nolimitsPrepDS]{nolimitsPrepDS()}} or \code{\link[=nolimitsPrepStringDS]{nolimitsPrepStringDS()}}.}

\item{theta}{the transformed-scale parameter vector, \code{sprintf("\%a", .)}-encoded
and comma-joined.
For \code{estimator = "map"} the returned \code{value} and \code{grad} carry the
LIKELIHOOD-ONLY part of the objective, and the log-prior is returned
separately as \code{prior.value} and \code{prior.grad}. The log-prior is data-free and
identical at every site, so a plain sum over sites would count it once per
site; the client adds the prior block exactly once. The prior block is a
function of the declared priors and the parameter transform alone and carries
no information about this server's data.

\code{"mle"} and \code{"map"} require a model with NO random effects. An analyst who
prepares a random-effects model and then asks for them gets the NoLimits
precondition error relayed as a FAILED message.}

\item{estimator}{one of \code{"laplace"}, \code{"focei"}, \code{"ghq"}, \code{"pooled"},
\code{"mle"}, \code{"map"}.}

\item{ghq.level}{Gauss-Hermite quadrature level, 1 to 9. Ignored unless
\code{estimator} is \code{"ghq"}.}

\item{round}{the federated round number, 1 to the \code{dsNoLimits.maxRounds}
option.}
}
\value{
A named list with \code{value}, \code{grad}, \code{natural}, \code{finite} and
\code{n.subjects}, plus \code{prior.value} and \code{prior.grad} when
\code{estimator = "map"}.
}
\description{
Aggregate method, and the hot path of the federated fit. The released
quantities per round are one scalar log-likelihood, one p-vector gradient, one
p-vector of natural-scale parameters (a deterministic model transform of the
argument, carrying no data), one flag and one subject count. Nothing whose
size depends on the number of observations or subjects is returned.
}
\details{
A non-finite objective is a legitimate estimator result at an out-of-domain
probe point, not a server failure: it is reported with \code{finite = FALSE} so the
client's line search can back off. Only genuine errors stop.
}
